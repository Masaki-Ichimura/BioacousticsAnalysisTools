#:import EditContainer app.gui.widgets.edit_container.EditContainer
#:import OffprocessContainer app.gui.widgets.offprocess_container.OffprocessContainer


<MainMenu>
    BoxLayout:
        orientation: 'vertical'
        size: root.size

        TabbedPanel:
            do_default_tab: False
            tab_width: 300

            TabbedPanelItem:
                text: '取り込み/編集'
                EditContainer:

            TabbedPanelItem:
                text: 'オフライン信号処理'
                OffprocessContainer:
